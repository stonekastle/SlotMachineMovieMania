
@{
    ViewBag.Title = "Scoreboard";
}

<div class="container-fluid familyMan">
    <center><input type="image" img src="~/Images/ATT Dan Created.gif" /></center>
    <div class="row">
        
        
        <div class="col-sm-12">
            <div class="Scoreboard" id="scoreboard">
                <h1 style="color:dodgerblue; font-family:verdana; font-size:300%; text-align:center;">Scoreboard</h1>
            </div>
        </div>
    </div>
</div>

<script type="text/template" id="template">
    <div class="container content">
        <div class="col-sm-4 image" style="color:brown; font-size:200%; text-align:right;">
        </div>
        <div class="col-sm-4 score" style="color:brown; font-size:200%; text-align:left;">
        </div>
    </div>
</script>

<script>
    $(document).ready(function () {
        getAll(onGetAllSuccess, onGetAllError)
    });

    var getAll = function (onGetAllSuccess, onGetAllError) {
        var url = "/api/scoreboard";
        var settings = {
            cache: false
            , dataType: "json"
            , success: onGetAllSuccess
            , error: onGetAllError
            , type: "GET"
        }

        $.ajax(url, settings);
        console.log("Loaded scoreboard successfully!");
    }

    var onGetAllSuccess = function (data) {
        for (var i = 0; i < data.Items.length; i++) {
            var $temp = $($("#template").html());
            $temp.find(".image").text(data.Items[i].ImageURL);
            $temp.find(".score").text(data.Items[i].Score);

            $("#scoreboard").append($temp);
        }
    }

    var onGetAllError = function (data) {
        console.log(data);
    }
</script>

